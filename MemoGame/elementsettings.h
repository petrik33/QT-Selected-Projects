#ifndef ELEMENTSETTINGS_H
#define ELEMENTSETTINGS_H

#include <QWidget>
#include <QPainter>

class ElementSettings : public QWidget
{
    Q_OBJECT
public:

explicit ElementSettings(QWidget *parent = nullptr);

public slots:
//    void setElementShape(ElementShape ElementShape);
//    void setPen(const QPen &pen);
//    void setBrush(const QBrush &brush);
//    void setTransformed(bool transformed);

protected:
//    void paintEvent(QPaintEvent *event) override;

private:
//    ElementShape ElementShape;
//    QColor color;
//    QPen pen;
//    QBrush brush;
//    bool transformed;
//    QPixmap pixmap;
};

#endif // ELEMENTSETTINGS_H
